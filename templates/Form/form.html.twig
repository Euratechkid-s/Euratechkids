{% extends 'base.html.twig' %}


{% block body %}

    <h1 class="text-center"> {{ titre }} </h1>
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        
        {{ form_widget(form)  }}
        <input type="submit"/>
        {{ form_end(form) }}




{% endblock %}